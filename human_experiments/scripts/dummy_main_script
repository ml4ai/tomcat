#!/bin/bash

# Assuming we are in a tmux session already
# which we SHOULD be

function monitor_log_file(){
    local tail_command="tail -f $1"
    tmux new-window $tail_command
}


log_path="output_file.txt"
nohup ./dummy_script &> $log_path &

monitor_log_file $log_path
