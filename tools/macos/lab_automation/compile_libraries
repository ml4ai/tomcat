#/bin/bash

if [ ! -d "compiled" ] 
then
	mkdir compiled
fi

#Compile config library
osacompile -o compiled/config_library.scpt config_library.scpt

#Compile spaces library
osacompile -o compiled/spaces_library.scpt spaces_library.scpt
